interface:
  display_name: "Japanese Writing Gemini"
  short_description: "小説・README・技術文書などをGeminiへ渡す自己完結ブリーフを作成"
  default_prompt: "$japanese-writing-gemini この日本語文章をGeminiへ依頼するための自己完結したブリーフを作ってください。"

policy:
  allow_implicit_invocation: true
